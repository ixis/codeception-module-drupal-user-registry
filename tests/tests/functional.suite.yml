#
# Configuration for Drupal User Registry functional tests.
#
class_name: FunctionalTester
modules:
    enabled: [Asserts, DrupalUserRegistry, FunctionalHelper]
    config:
        DrupalUserRegistry:
            roles: ['administrator', 'editor', 'moderator', 'Authenticated']
            password: 'test123!'
            create: false
            delete: false
            drush-alias: '@d7.local'
            root:
              username: root
              password: root
